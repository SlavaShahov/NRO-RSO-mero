# RSO Events Mobile (Flutter MVP)

## Run

```bash
flutter pub get
flutter run
```

Default API URL in app: `http://10.0.2.2:8080` (Android emulator to host machine).

For iOS simulator, replace base URL with `http://localhost:8080`.
